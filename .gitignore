# Personal Claude Code overrides - never shared
.claude/settings.local.json

# Secrets - mirrors the deny-read list in .claude/settings.json, so the
# two stay in agreement.
.env
.env.*
!.env.example
*.pem
*.key
credentials.json
secrets.yml
secrets.yaml

# .NET build output
bin/
obj/
*.user
.vs/

# Downloaded/vendored binaries (re-fetchable; not source)
lib/pawnio/
lib/lhm/
*.zip

# Installer / release output
dist/
*.msi
publish/

# Vendored tools + scratch (re-fetchable; not source)
tools/
work/

# OS cruft
Thumbs.db
desktop.ini
